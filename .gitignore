__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/
.DS_Store

# Real secrets should never be committed — EnvGuard exists to catch this,
# but ignoring the common ones by default doesn't hurt.
.env
.env.local
.env.production
